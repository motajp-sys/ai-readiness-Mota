git add
This command will add a new file to a repository.

git commit
This command will save your changes to the repository.

git push
This command will take whatever changes you've made locally and send them to your remote repository (in this case, Github).

